# Dogfood — intent-engineering v0.8.0 (plugin on itself)

**Date:** 2026-07-30  
**Branch:** `feat/ie-init-wizard-0.8`  
**Scope:** new setup surface (ie-init wizard, conventions.auto, severity_align,
patterns.preferred, ie-from-pr-learnings) + release docs/site  
**Method:** contract suite + IE self-review (predictability/convention/simplicity) +
compound-engineering style doc review (coherence / feasibility / product)

## Results

| Check | Result |
|-------|--------|
| `ruby scripts/check-contracts.rb` | **PASS — 141 checks, 0 failures** |
| Defaults vs wizard promises (keys, evals, versions) | **DOGFOOD_OK** |
| IE dogfood (explore subagent) | Fixes applied for P1/P2 |
| CE doc review (headless) | Fixes applied for P1/P2 |

## Findings fixed before PR

1. **severity_align over-broad match** — require smell match when a theme lists smells;
   drop bare `architecture` promote on callback/migration gates alone.
2. **Product-specific default `exclude` globs** — replaced with generic
   labeler/dependabot/deploy/terraform-style noise only.
3. **Preferred + blocked double findings** — init sets preferred-only for interactor
   stance; architecture agent emits one finding when both would fire.
4. **Init required vs optional** — plugin README states defaults work without `/ie-init`.
5. **Non-interactive multi-repo** — tokens `multi-repo` and `roots:a,b`.
6. **Doc drift** — six skills in layout/maps; framework doc count 15; calibrate not
   re-credited as new; dogfood report path real; convention authority order clarified.
7. **Site copy** — six skills framed as four review contexts + setup + PR learnings.

## Residual / post-merge

- GitHub release tag `v0.8.0` and site release links go live after merge + tag.
- STATUS may say release candidate until the tag exists; version numbers already 0.8.0.
- Full interactive `/ie-init` on a throwaway multi-repo sandbox is still useful after install.

## Verdict

**Ready for PR** on the 0.8.0 feature set after the fixes above. Contract suite green;
setup path is documented for monolith, multi-repo, and upgrade.
